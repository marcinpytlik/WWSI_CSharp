# Zadanie 02 – Model Student / Person

**Termin:** tydzień 5

## Treść

`Person` (Name, BirthYear) i `Student : Person` (IndexNumber, GPA).
Metoda `Age(int year)` ; GPA w zakresie 0–5, inaczej wyjątek.

## Kryteria

- [ ] hermetyzacja (brak publicznych pól)
- [ ] 5 testów, w tym zły GPA i polimorficzne `Person p = new Student(...)`
